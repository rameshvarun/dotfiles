# This is simply a list of Atom packages I like to keep installed, in order to
# bootstrap my environment on new computers.

apm install autocomplete-python # Intelligent auto-complete using Jedi
apm install file-icons # Lots of cool file icons.
apm install pigments # Highlight colors found in code.

apm install linter # UI for linters.

apm install language-lua
apm install linter-luaparse # Lua linter that doesn't require Lua to be installed.

apm install make-executable
